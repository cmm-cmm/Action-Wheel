// The business-logic layer. Every window and service in this project talks to it, so importing it
// once here keeps the layering visible in the project file rather than repeated at the top of
// thirty source files.
global using Action_Wheel.Core;
